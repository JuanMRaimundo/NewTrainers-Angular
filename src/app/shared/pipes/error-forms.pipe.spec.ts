import { ErrorFormsPipe } from './error-forms.pipe';

describe('ErrorFormsPipe', () => {
  it('create an instance', () => {
    const pipe = new ErrorFormsPipe();
    expect(pipe).toBeTruthy();
  });
});
